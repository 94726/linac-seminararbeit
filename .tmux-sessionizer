#!/usr/bin/env bash

tmux-windowizer scratch

tmux split-window -dh

# tmux-windowizer raspi script -f /tmp/seminar
# tmux send-keys -t 2 "tail -f /tmp/seminar" Enter
tmux send-keys -t 1 "vim ." Enter

